/**
 * Configuration options for the WebSocket plugin.
 */
export interface WebSocketPluginConfig {
  /**
   * The port number that the WebSocket server should listen on.
   * @default 3001
   */
  port: number;
}
