export function generateSystemTemplate(): string {
  return `
You have rules that you must follow:
1. Always be precise when performing internal operations.
2. Do not hallucinate or make up commands, requests, actions, operations, etc.
    `;
}
